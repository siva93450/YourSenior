# Database connection
spring.datasource.url=jdbc:mysql://localhost:3306/your_senior_db
spring.datasource.username=root
spring.datasource.password=SIVA@12345

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Server
server.port=8080

# App name (optional, shows in logs)
spring.application.name=your-senior